# Hermes Task: Phosphene Snapshot v0.1.10 Operator Demo

## Context

You are Hermes and you work on the AI Node. You do not have access to Reyk's local development machine or local repository.

Phosphene v0.1.10 can load a published redacted snapshot from `/snapshots/current/` and displays a `Published AI Node Snapshot` status panel with source, classification, manifest size, validation state, and the explicit `No live telemetry` boundary.

This is not a live telemetry task. Do not read live Hermes, AAG, OpenClaw, Sentinel, Gmail, Google Workspace, provider, customer, credential, or private infrastructure data. Do not execute live side effects.

## Goal

Create a fresh synthetic, fully redacted Phosphene Boundary snapshot pack for the v0.1.10 operator demo.

## Output Directory

Container path:

```text
/opt/data/phosphene-handoffs/boundary-v0.1.10/hermes-snapshot-2026-06-20-operator-demo/
```

Host-equivalent path:

```text
/Users/raik./ai-stack/data/hermes/home/phosphene-handoffs/boundary-v0.1.10/hermes-snapshot-2026-06-20-operator-demo/
```

Create the directory if it does not exist. Replace only files inside that output directory.

## Required Files

Generate exactly these four trace bundles:

- `hermes-aag-gmail-draft.synthetic.json`
- `hermes-openclaw-worker.synthetic.json`
- `aag-workspace-bundle.synthetic.json`
- `sentinel-recovery.synthetic.json`

Also generate:

- `manifest.json`
- `validation-report.json`
- `README.md`

The manifest must list all four trace bundle filenames in `files`.

## Boundary Contract

Use Boundary schema version:

```text
phosphene.boundary.v0.1.2
```

Allowed sources:

```text
hermes, openclaw, aag, sentinel
```

Allowed event types:

```text
run.started
agent.plan
tool.requested
tool.executed
aag.decision
approval.required
worker.started
worker.completed
health.check
sentinel.alert
sentinel.recovery
run.completed
```

Allowed statuses:

```text
queued, running, needs_approval, approved, denied, succeeded, failed, recovered
```

Allowed risks:

```text
low, medium, high
```

## Trace Content Requirements

Make the traces clearly different from previous static examples while staying synthetic and redacted.

Each trace must have:

- `schema_version: "phosphene.boundary.v0.1.2"`
- `metadata.id`, `metadata.title`, `metadata.subtitle`, and `metadata.status`
- exactly one root event where `parent_event_id` is `null`
- every non-root `parent_event_id` references an event in the same file
- at least 4 events and at most 7 events
- summaries that are human-readable and explicitly synthetic/redacted
- no real email addresses, URLs, provider IDs, credentials, tokens, customer data, message bodies, document content, or private infrastructure details
- `redacted_payload_hash` values only in the shape `sha256:redacted-...`
- `links` using `trace://...` only, never HTTP URLs

## Suggested Scenario Emphasis

- Hermes -> AAG -> Gmail Draft: synthetic operator note draft is held for approval.
- Hermes -> OpenClaw Worker: synthetic bounded worker summarizes a redacted diagnostic bundle.
- AAG Workspace Bundle: synthetic multi-action workspace preview is blocked pending approval.
- Sentinel Recovery: synthetic health gate detects a queue stall and records recovery.

## Manifest Requirements

`manifest.json` must include:

- `schema_version: "phosphene.boundary.v0.1.2"`
- `created_at` around `2026-06-20T11:45:00Z`
- `source_agent: "hermes"`
- `data_classification: "synthetic_redacted"`
- `files` listing the four trace bundles with descriptions
- `import_contract: "phosphene.boundary.v0.1.2-importable"`
- `notes` saying this is a v0.1.10 operator-demo snapshot and not live telemetry

## Validation Report Requirements

`validation-report.json` must include:

- `schema_version: "phosphene.boundary.v0.1.2"`
- `overall_status: "passed"`
- `data_source: "synthetic_files_only_no_live_reads"`
- one `trace_results` entry per trace file with `status: "passed"`

## README Requirements

`README.md` must state:

- generated by Hermes on the AI Node for Phosphene v0.1.10 operator demo
- synthetic/redacted only
- not live telemetry
- no live private data was read
- host publication should use `/Users/raik./ai-stack/scripts/publish-phosphene-snapshot.sh <host-output-dir>`

## Hard Boundaries

- Work only on the AI Node.
- Do not deploy Phosphene.
- Do not publish the pack yourself.
- Do not commit to any repository.
- Do not open ports.
- Do not execute live side effects.
- Do not read or emit secrets, bearer tokens, OAuth tokens, API keys, private keys, session cookies, private URLs, raw provider IDs, real email addresses, real message bodies, real document content, real calendar data, customer data, provider payloads, or private infrastructure details.

## Completion Report

Report back:

- output directory path
- generated file list
- confirmation that the pack is synthetic/redacted only
- confirmation that no live private data or side effects were used
